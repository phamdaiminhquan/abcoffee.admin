# Base URL cho API client.
# Dùng proxy Vite (vite.config.ts) khi đặt là /api.
# Muốn gọi trực tiếp backend thật (bỏ qua proxy) thì sửa thành: http://localhost:3000
VITE_APP_API_BASE_URL=/api

# Bật/tắt mock (MSW). Khi test với backend thật đặt false.
VITE_ENABLE_MSW=true

VITE_APP_DEFAULT_ROUTE=/workbench
VITE_APP_PUBLIC_PATH=/
VITE_APP_ROUTER_MODE=frontend
